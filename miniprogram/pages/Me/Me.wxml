<view class="user">
  <view class="user_info">
    <image src="{{userPhoto}}"/>

    <text wx:if = "{{logged}}" >你来啦！{{nickName}}</text>
    <!-- 获取用户信息登录 -->
    
    <!-- 若成功后可删除下 -->
    <button  wx:else disabled = "{{disabled}}" open-type="getUserInfo" bindgetuserinfo="onGotUserInfo">登录</button>
  </view>
  
<view class="contact">联系我们
</view>
</view>
<!-- </view>

    <text>{{nickName}}</text>

    <button wx:if="{{canIUse}}" open-type="getUserInfo" bindgetuserinfo="onGotUserInfo">登录</button> -->
 


  <!-- <image class="image" src="{{userinfo.avatarUrl}}"></image>
  <view class="name">{{userinfo.nickName}}</view>

<view wx:if="{{!openid}}">
  <button open-type="getUserInfo" lang="zh_CN" bindgetuserinfo="onGotUserInfo">登录</button>
</view> -->